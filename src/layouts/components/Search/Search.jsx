import React from "react";
import styles from "./Search.module.scss";

const Search = () => {
  return (
    <form>
      <input type="text" />
      <button>Найти</button>
    </form>
  );
};

export default Search;
